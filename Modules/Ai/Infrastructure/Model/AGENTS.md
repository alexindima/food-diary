# Ai module guidelines

Own four Ai EF configurations and internal quota ledger records. Preserve internal visibility via explicit friend assemblies. ApplyAiPersistenceModel is called by central DbContext. Preserve schema, DbSets, CLR names and migrations; quota records consume the existing reservation port and must not introduce a Domain-to-Application dependency.

# Hydration Infrastructure Guidelines

Hydration repository implementations and the complete `AddHydrationModule` composition facade live here. Reuse central `FoodDiaryDbContext`; central Infrastructure must never reference this project. Migrations remain central.

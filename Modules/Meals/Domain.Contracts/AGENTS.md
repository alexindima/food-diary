# Meals scalar contracts

Own stable Meals IDs, plus meal enums. Preserve CLR namespaces, numeric values and ID conversions. Depend only on shared domain primitives. Aggregates, mutable state, events, repositories, EF and application behavior stay outside this project. Actual consumers reference this owner directly.

# Meals scalar contracts

Own stable Meals IDs, plus meal enums. Use canonical project and folder namespaces; preserve numeric values and ID conversions. Depend only on shared domain primitives. Aggregates, mutable state, events, repositories, EF and application behavior stay outside this project. Actual consumers reference this owner directly.

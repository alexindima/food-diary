# OpenFoodFacts Application Abstractions Guidelines

Provider and durable-cache ports live here. Preserve legacy CLR namespaces. Depend only on module Contracts; do not reference EF Core, provider transports, hosts, or implementations.

# OpenFoodFacts Application Abstractions Guidelines

Provider and durable-cache ports live here. Use canonical project and folder namespaces. Depend only on module Contracts; do not reference EF Core, provider transports, hosts, or implementations.

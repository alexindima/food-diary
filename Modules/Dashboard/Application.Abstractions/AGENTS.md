# Dashboard projection ports

Own the snapshot/body/meals ports and their projection/section models. Use folder-aligned namespaces. Reference Contracts, UserId and Results only; no EF, HTTP, Application
implementation or central Abstractions dependency. Projects must remain siblings with no nested compilation exclusions.

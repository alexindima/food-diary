# Dashboard tests

Application tests own snapshot composition/fallback/validation. Their canonical assembly identity has explicit friend grants for existing internal neighboring read-service fixtures; production consumes narrow contracts. Infrastructure tests own the four projection readers.
Own Dashboard DI registration and UTC date-normalization tests here. Mixed composition, HTTP and shared PostgreSQL fixture tests stay with their established owners.
Use normal dotnet test, without collectors. Do not add wall-clock performance gates.

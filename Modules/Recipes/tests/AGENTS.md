# Recipes tests

Module application tests own existing Recipes use cases and calculation behavior. Preserve legacy test namespaces; every test/helper has ExcludeFromCodeCoverage. Mixed central PostgreSQL fixture, Product advisory-lock coordination, Favorites, HTTP, DI and central Domain Meal snapshot suites remain with current owners. No test duplication or cross-test-project references. Run ordinary tests without coverage collectors and save VSTest TRX/counts.

# Recipes read contracts

Own RecipeErrors with unchanged error codes, messages and kinds; consumers must
not reference internal Abstractions merely for error factories.

Own existing access, lookup, overview services and projection/filter records. Preserve CLR namespaces and signatures. Return projections, never foreign aggregate mutation rights. Central Domain IDs/enums remain compatibility types. No handlers, EF, HTTP or providers.

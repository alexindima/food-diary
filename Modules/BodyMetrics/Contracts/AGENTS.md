# BodyMetrics consumer contracts

Own weight/waist read services and their four immutable entry/summary models.
Preserve WeightEntries and WaistEntries CLR namespaces, user scoping, ordering,
limits, date boundaries and quantization semantics. Depend only on scalar UserId.
Repository ports and aggregate entities remain internal module dependencies.

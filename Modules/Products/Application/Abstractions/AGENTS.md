# Products Application/Abstractions

Owner-only aggregate repositories, mutation transaction port and errors. Depend on Products Domain, Users Domain.Contracts and Results; preserve CLR namespaces. Never expose aggregate repositories to foreign modules.

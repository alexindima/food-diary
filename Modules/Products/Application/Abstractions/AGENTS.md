# Products Application/Abstractions

Owner-only aggregate repositories, mutation transaction port and errors. Depend only on central Domain and Results; preserve CLR namespaces. Never expose aggregate repositories to foreign modules.

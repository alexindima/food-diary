# Shared nutrition contracts

Own only stable nutrition constraints that are intentionally shared by multiple
bounded contexts. Do not grow this into a generic domain or application bucket;
module-specific calculations and validation rules remain with their owners.

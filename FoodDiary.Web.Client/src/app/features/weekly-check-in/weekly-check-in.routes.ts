import { type Routes } from '@angular/router';

import { WeeklyCheckInPageComponent } from './pages/weekly-check-in-page.component';

const routes: Routes = [
    {
        path: '',
        component: WeeklyCheckInPageComponent,
    },
];

export default routes;

export type EntityCardConfig = {
    collage: {
        visibleLimit: number;
    };
};

export const DEFAULT_ENTITY_CARD_CONFIG: EntityCardConfig = {
    collage: {
        visibleLimit: 4,
    },
};

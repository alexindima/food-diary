# RecipeCommunity ownership inventory (pre-move)

Base: 3851a98e0340fd12191c56cf41f261a678a632c8. Clean detached worktree; branch codex/recipe-community-module.

- Application: RecipeComments create/update/delete/get slices, validator, read service/model; RecipeLikes set-state/status slices and read service/model; DI and assembly marker.
- Ports: central RecipeComments/Common (four repository interfaces and errors), Models/RecipeCommentReadModel; RecipeLikes/Common (three repository interfaces). Central Errors.RecipeComment remains a compatibility facade delegating to owned errors.
- Domain: RecipeComment + RecipeCommentId; RecipeLike + RecipeLikeId. No owned enum/event/provider/job. Both User navigations and comment Recipe navigation are one-way: rg finds no references to either entity or ID elsewhere in central Domain. EF WithMany() has no inverse CLR navigation. Extract Domain with a one-way central Domain dependency; preserve namespaces, IDs and properties. Do not extract Recipe/User.
- Persistence: RecipeSocial configurations and RecipeComments/RecipeLikes repositories, central DependencyInjection.RecipeInteractions aliases. Central DbContext, migrations and snapshot stay. Explicit model assembly registration prevents missing mappings.
- Contracts: no separate stable external capability is justified; application read-service/model remain used by own queries and presentation. Application.Abstractions owns ports. Central facade dependency points inward to extracted ports, never back to central abstractions.
- External consumers: API/Initializer composition, Presentation controllers/mappings, JobManager existing project reference; ContentReports reportability reads comment.Recipe.Visibility/owner via shared context; user cleanup and recipe reads stay with owners. Recipes access uses IRecipeAccessService/RecipeOverviewReadItem; acting user uses ICurrentUserAccessService.
- Notification seam: CreateRecipeCommentCommandHandler persists comment then calls INotificationWriter for a different owner with NewComment + empty payload + recipe ID. Existing transaction, provider and shared outbox code unchanged.
- Likes: set-state command checks existing row and adjusts count only when desired state differs; unique (UserId, RecipeId) EF index. Preserve sequential idempotence and existing concurrent-unique-conflict behavior, not a new upsert policy.
- Focused tests: RecipeCommentsFeatureTests, RecipeCommentsValidatorTests, RecipeLikesFeatureTests move from application donor; RecipeCommentInvariantTests move from domain donor. SocialInvariantTests mixes ContentReport and RecipeLike and stays central. Presentation tests stay central. AdditionalPersistenceRepositoryIntegrationTests mixes modules and retains RecipeSocialRepositories_AddQueryUpdateAndDeleteLikesAndComments; PersistenceRepositoryCoverageIntegrationTests covers ContentReport reportability.
- Wiki FN: generated RecipeCommunity page declares no entities/HTTP and only architecture focused tests; source paths omit actual entities and repositories. Correct ownership manifest from sources; no module-specific generator special case.

# Products extraction: Wiki evaluation and evidence

Base `4c1b1c2c31a0886cf7e01d8bba7ec00231e6b149`, clean initial worktree
`C:/Users/alexi/.codex/worktrees/39a1/FD`; branch `codex/products-module-extraction`.
No master/neighbor edits, push, production/SSH or coverage collectors.
Ownership and compatibility: [inventory](products-ownership-inventory.md).
Evidence lives in `.artifacts/products-evidence`; disposable builds use only
`.artifacts/products-extraction`. Heavy commands hold the shared FileShare.None
slot and preserve logs outside the native-clean scope.

## Discovery evaluation

- Root/scoped guidance and ADRs 0006/0007/0008/0009/0016 supplied ownership,
  read-model, transaction and physical-module boundaries. Direct sources proved
  the Product/User/RecipeIngredient/MealItem/USDA central CLR seam. No empty Domain
  project or foreign aggregate redesign is justified.
- `start` received a real PlannedPath array, captured zero pre-existing paths,
  exited 0 and selected architectural governance: 57 grounded paths, 7 phases,
  12 criteria. Its initial evidence bundle contained zero checks/reviews; this
  is navigation/planning, not execution evidence.
- Compact `research` found ports and requested a design boundary. Source-backed
  decisions produced `design ready=True`, preserving schema, transactions,
  provider/privacy ownership and coordinated rebuild compatibility.
- `brief`, exact-source `test-plan` and `decision` exited 0. Test planning found
  application and PostgreSQL obligations, but also unrelated frontend cards and
  period filters. These are not justification for changing frontend behavior.
- Generated Products page says zero DTO/read-model/projection types despite
  listing ProductOverviewReadItem and ProductQueryFilters records. It also omits
  ProductLookupService from source areas and does not explain central inverse
  navigations. `extracted-project` is the legacy Application split, not proof of
  a completed physical logical module.
- Before edits, native task-contract contained `^Modules/Products$`, which does
  not allow future descendants. `task-contract-before.json` preserves it.
  Native contract initialization expanded this task's inventory scope; the
  shared generator, policy and fingerprints were not edited.
- Cold worktree: `ownership` advertised JSON fallback but failed in nested
  Get-LlmWikiDiffContext.ps1:160 with SQLite compiled-index-projection-missing.
  One explicit Json attempt reproduced the exception. Both attempts are retained
  and were reported to the parent; no retry loop or generic tool repair occurred.
- `topology`, `privacy`, `dependencies`, `rollout` and `trace` exited 0 using
  explicit JSON. Topology included unrelated global providers; privacy returned
  zero focused candidates, not proof of absence. Direct source review established
  unchanged private visibility, owner-only mutation, media/outbox, provider and
  cache behavior. The asynchronous trace completed after relocation and correctly
  located the new Products handler, mutation runner, tests and HTTP mappings;
  it is post-move navigation evidence, not a pre-edit trace certificate.
- Six fixture literals were migrated across four eval files and two test scripts.
  `fixture-path-audit.json` records Git/source equality after LF normalization and
  source SHA256 for each replacement. Query text, ranking, assertions and thresholds
  are unchanged. The wider production audit proves 57 of 58 relocated C# files
  unchanged after LF normalization; only Application DI renamed its registration
  method to make room for the complete infrastructure composition facade.
- Native `delivery-replan` also failed with compiled-index-projection-missing and
  reported atomic workspace restoration. It needs a bounded rerun after ordinary
  graph prerequisites are restored; it is not a completed governed phase.

## Verification status

Implementation and verification are in progress. Final process exits, test counts,
EF result, Wiki quality and governed-delivery gaps will be recorded here before
handoff. The supplied master quality failure (frozen100 top10 96/100,
acceptedPrecision .9481, errorCaptureRate .5) is context, not a measured result for
this branch and not a waiver. No ranking, query, threshold or frozen-history
change is authorized by this extraction.

Initial force/locked restore both passed. Initial full build failed with 37
diagnostics: missing shared test props, mixed OFF dependencies and unused usings
from the split. No tests ran. A second force/locked restore passed; the bounded
module build then found one leftover unused OFF test helper. Both attempts ran
native clean successfully and released the slot. These are extraction defects,
not baseline failures. The mixed OFF/USDA provider tests now remain central and
the unused helper is removed; subsequent verification must establish the result.

The third force/locked restore passed, but the module build failed with 31
diagnostics: two omitted image-access test helpers and redundant blank lines.
`runtime-20260831-135059.json` and its full build log retain the failure; native
clean exited 0. No test execution or baseline claim follows from this attempt.

Before another build, a unified source packaging audit compared all Products
test methods against Git HEAD: 114 original methods, 103 module methods and 11
mixed donor methods, no missing/extra methods and no changed method bodies after
whitespace normalization. This is source preservation evidence, not test counts
from a runner. The three Support files are identical to donor sources. The
module imports the same shared test props (CodeAnalysis global using, assembly
coverage exclusion, runsettings and xunit configuration), retains Xunit,
NSubstitute and Support global usings, and inherits the same root analyzer and
framework settings. SDK default source inclusion owns the complete partial
family and helpers; there are no linked C# sources or test-project references.
The four direct test packages retain central versions; FluentValidation and
production contracts remain available through the application reference graph.
No collector runs are used. The two missing helpers and whitespace were fixed
together, and unused helpers in the remaining mixed suggestion class removed.
The pre-existing reflection setup for Product usage collections is unchanged;
no new reflection access or production public surface was added for tests.
See `test-packaging-audit.json`, `test-method-audit.json` and
`test-body-audit.json` in the evidence directory.

A final source-to-contract check found the already edited JobManager/Program.cs
missing from allowed paths. Native task-contract initialization added that exact
host path, preserving before/after evidence; this is a corrected scope omission,
not proof that the earlier edit had complete pre-edit governance. The current
contract includes module descendants and the audited fixture paths. The final
native replan/validation must still establish governed continuity.

Attempt `20260831-140020`: force/locked restore exited 0; module build exited 0
with zero warnings/errors (15.16 seconds). Full solution build exited 1 with
two IDE0005 diagnostics in remaining donor ReadQueryTests (unused GetProducts
and Common.Models imports). Both imports were removed. The static packaging
audit missed these donor imports; it is not a compiler substitute. No tests,
npm or graph commands ran in this attempt; native clean exited 0 and released
the lock. Full diagnostics and command durations remain in its runtime JSON/logs.

A later ownership pass corrected an inventory omission: the three tests in
ProductRepositoryIntegrationTests exercise Products-only search escaping, filters
and first-page latency. Shared fixture usage is not itself a mixed-module test.
They now physically belong to a module Infrastructure.IntegrationTests project;
test source is unchanged. Two shared PostgreSQL fixture sources remain central
and are linked using the established Cycles precedent, with FoodDiary.Testing
and module Infrastructure references only. No Initializer reference is needed
by this fixture/test source. This test project uses the shared test props and
explicit existing package versions; it adds friend access to the already internal
read adapter, not public API or reflection. Full central PostgreSQL execution is
still required separately; moving three tests must not masquerade as skipping
them. The exact donor path was added to the native contract before this move.

The final staged production rename audit includes ports/contracts and the former
central registration file as well: 69 C# rename pairs, 67 unchanged after LF
normalization. The only changed pairs are Application registration and the full
module registration facade. This supersedes the earlier narrower 58-file count;
`production-rename-audit.json` retains every old/new path and source hash.

Attempt `20260831-141926` stopped at force restore with NU1903: omitting the
donor's direct SSH.NET reference let the new PostgreSQL project select vulnerable
transitive 2025.1.0. The central package file already pins 2026.0.0. Restoring
that direct reference, as in donor/Cycles, preserves the existing safe override;
no package version or warning policy was changed. The source-only packaging audit
missed this transitive dependency effect. Native clean exited 0; no build/tests
ran. The failed restore log is retained separately from subsequent restore results.

## Runtime pass 20260831-142035

Force-evaluate and locked restore passed. Both module test projects, donor
Application.Tests and the full solution built with zero warnings/errors; full
solution build took 105.91 seconds. npm ci exited 0 but reported three high
vulnerabilities, with frontend lock/package files unchanged; no audit fix ran.
Existing 261 NuGet lockfiles retain their resolved versions. Own Wiki update
exited 0 (75.47 seconds), graph-build exited 0 (7159 files/31628 symbols).
One bounded ownership rerun then exited 0. Native delivery-replan exited 0 with
applied=true, 282 paths/7 phases/6 checks; unresolved obligations stayed pending.
The regenerated Products page locates owned PostgreSQL tests but still reports
DTO count zero and omits Infrastructure from Source Areas.

Sixteen unfiltered suites produced 5193 passed, 2 failed, 0 skipped cases.
Application: Products128, donor898, Recipes63, Favorites78, MealPlanning104,
USDA27, OpenFoodFacts14; Domain877; Infrastructure unit651; JobManager168;
Presentation825; WebApi unit247; Products PostgreSQL3; central PostgreSQL114;
full HTTP182. Architecture was 814/816: the guard retained the old
ProductLookupService path, and manifest discovery saw an empty obsolete Products
donor directory. Both failure records remain in TRX/JSON. The guard now points
to the actual module source; only three confirmed empty donor directories were
removed non-recursively after the block. No assertion, budget or filter changed.
Architecture must rerun in full. The PostgreSQL total remains 117 across owned
and central suites; moving cases did not remove them from execution.

EF has-pending-model-changes exited 0 with no changes. Its existing tools
10.0.10/runtime10.0.11 warning was retained, without a version update. NuGet audit
exited 1 because the wrapper restored ArtifactsPath after EF and the list command
looked for default assets: this was not a vulnerability result. It needs a rerun
with the correct artifact environment. Native clean exited 0 and released the
slot; logs and all 16 TRX hashes are in runtime-summary-20260831-142035.json.

The impact facade printed text despite Format Json (its documented machine
route is Get-LlmWikiImpact.ps1); native structured impact identifies 20 manual
pages requiring review. Their affected claims were reviewed against source
diffs and the new inventory; Products placement was added to primary-backend.
The broad code-graph workflow review covers the affected corpus/path-migration
claims, not a fresh certification of historical benchmark numbers. A policy
invocation with the unsupported facade FailOnViolation switch failed at binding;
the correct policy facade invocation exited 0. These invocation diagnostics
remain separate from semantic validation.

The final focused-test ownership audit also identified the standalone
ProductInvariantTests as movable independently of the production Domain seam.
Its 55 Fact/Theory methods and local helper moved unchanged into a third owned
test project referencing only central Domain. No new production Domain project,
public API, friend access or reflection is needed. The source hash/equality
record is domain-test-packaging-audit.json. The earlier central Domain877 result
predates this packaging move; both resulting Domain projects must run before
final handoff. The shared/mixed Domain suites stay central. This corrects the
earlier assumption that central production placement requires central placement
for every focused test. Native task scope was extended before the move.

A final literal-path scan before the next heavy block found one additional stale
Products target in context-search-probe.json (product-lookup-service-probe).
Native scope was expanded before the edit. The old ProductLookupService Git
source and new module source are LF-identical; only expectedPaths changed.
The fixture audit now contains seven literal replacements across five eval files
and two tool fixtures. No query, ranking, threshold or historical result changed.

## Final runtime verification 20260831-145301

Force-evaluate restore and locked restore exited0. Full solution build exited0,
zero warnings/errors,155.34s. Five unfiltered suites passed2720/2720 with0skips:
Products Application128, donor Application898, Products Domain70, donor Domain807,
complete Architecture817. The earlier two architecture failures are resolved by
exact source path correction and removal of confirmed empty donor directories.
The new Domain split preserves877 total cases. EF again exited0 with no pending
model changes; the existing tools/runtime version warning remains disclosed.
NuGet audit used this worktree's ArtifactsPath and exited0; JSON covers266projects,
zero vulnerable package entries. Both restores preserve central pinned versions.

For the final runtime ledger, select the latest result per suite, never sum
historical reruns:17suites,5196passed/0failed/0skipped. This includes the other12
unchanged suites from142035: Recipes63,Favorites78,MealPlanning104,USDA27,OFF14,
Infrastructure unit651,JobManager168,Presentation825,WebApi unit247,ProductsPG3,
centralPG114,fullHTTP182. No filters or collectors were used. The complete central
PostgreSQL114 result legitimately supports both infrastructure policy IDs; owned
ProductsPG3 is additional execution, preserving the original117cases.
Native clean exited0 and released the shared slot. TRX/logs and hashes remain in
products-evidence, outside build output.

Own Wiki update,graph-build,source review refresh and architecture-health exited0.
Native final replan exited0/applied=true with287paths/7phases/6checks. Actual
`wiki verify` exited1 at affected smoke: TestPlanPrecision line52 retained two
stale Billing paths. The original ui-continuation log is preserved in
`.artifacts/llm-wiki/parallel-smoke/64256-9ba02c84a1384322a5eda0085e6e631d/`.
Parent approved exactly two additional fixture literals after verifying Git
037bbc254b80b5e8c2cd683183fbec2cb4aa4315 R100 history. Independent LF equality
checks are saved as billing-fixture-path-audit.json. Assertions,Intent,ranking,
thresholds and generators are unchanged; targeted rerun remains required.

The concurrently running adaptive-evals group separately reports shard0 exit1
and shard2 exit-1. Its wrapper does not redirect or persist per-child stdout/stderr;
retained transcripts contain no exact shard0 assertion. The recorded cancellation
reason is group-failed:ui-continuation. Interrupted shard2 is not a semantic
result. Do not infer a baseline or claim the entire adaptive group passed.

A separate native frozen100 snapshot ran under the shared lock after verification,
with1measured iteration/0warmup and SkipBuild. Process exit0, but semantic
liveRegressionPassed=false: top1=92/100,top10=96/100,MRR=.9345,
acceptedPrecision=.9481,errorCaptureRate=.5. Those metrics match the supplied
master figures; this does not establish a baseline for the separate smoke failure.
The corpus and ranking policy are unchanged; the snapshot includes their hashes,
Git base and matching graph/worktree fingerprint. Source changed only later for
the parent-approved Billing fixture correction, so this is point-in-time evidence,
not a certificate for a subsequent diff. Native snapshot clean exited0/released.
`frozen100-current.json` and generated Markdown preserve actual provenance.

## Bounded diagnostic run 20260831-150417

Parent authorized one directly captured shard0 and targeted TestPlanPrecision
under the shared lock after the two Billing literal corrections. Own graph-build
exited0. TestPlanPrecision still exited1 at line52 (renewal test selection);
correcting stale paths alone did not resolve the navigation defect. No additional
assertions or ranking logic changed. Direct adaptive shard0 exited1,127/129
assertions passed: repository-performance missed performance-data-access and
its data-access-integration-tests requirement. The policy matches only central
FoodDiary.Infrastructure/Mail persistence paths, not Modules/Products. This is a
confirmed modular path policy gap, not permission to drop the expected guard.
A bounded additive Products path proposal was sent to the parent; no policy edit
was made at this point. Old failed stage logs remain independent evidence.

Source inputs were held fixed. Native graph before/after both report fresh and
identical change-set fingerprint f7bf7f93758b4d7525ef21255512b9a21c643cf6cb053db63a430351aad5dea8.
The separately recorded native content fingerprint uses directory markers for
its eval/policy directory arguments, so it must not be represented as recursively
hashing those descendants; its two exact tool file hashes are valid. The21-file
eval audit and full graph fingerprint are additional evidence. Logs include
actual arguments/start/end/exit. Native clean exited0 and released the lock.

The parent subsequently authorized a generic compatibility repair limited to the
existing performance-data-access rule: two module-root analogues of its central
patterns, requiring one module segment and exact Infrastructure. All old patterns,
exclusions,check commands,review obligations,id and description remain unchanged;
module-policy-delta-audit.json proves JSON equality after removing only the two
additions. Native scope was extended before edits. A standalone deterministic
policy-evaluator regression covers Products/Recipes/Dashboard and legacy/Mail
positives plus wrong layers,InfrastructureExtra,nested module segments,tests and
Migrations negatives; it verifies both matched rule and required check. Its
execution is pending the next agreed slot. No test selection/ranking repair was
made for the separate Billing navigation failure.

## Policy repair verification and native delivery

Under the next agreed lock, the18-case module persistence policy regression
passed, and exact adaptive shard0 passed10/30cases,129/129assertions. Both process
exits were0. All341 explicitly enumerated tool/eval/policy files had identical
before/after fingerprints (7411503ac8a971f90d2c43f4fa0599642313be33b3b53be4aef25a8c2c7f0ff5),
with zero directory markers. Graph receipts and actual start/end/commands are in
policy-targeted-runs-20260831-151244.json. Native clean exited0/released. This
repairs one proven policy-layout defect; it does not establish full Wiki success.
The Billing selection assertion remains unresolved, and no further full verify
or interrupted-shard success is claimed.

The final runtime ledger selects17latest suites,5196passed/0failed/0skipped.
Its explicit current .NET source/project/build/lock manifest fingerprint is
94a77648a0c7144091791b6435d909cf6285f2332d77d41d5c0541ebd2177360.
It is a current audit, not a retroactively captured run-start fingerprint; only
Wiki policy/tool/docs changed after the final runtime run.

Native acceptance was split into18atomic criteria,16satisfied and2migration
criteria not applicable. A first validate/critique rejected missing ChangedPath
links and unresolved Wiki verification; critique also warned about absent context
trust assessment. Exact links were restored through native mapping, requirements
sealed, and context-security evidence created. Original rejection logs remain;
subsequent native gate outputs, rather than this narrative, determine final status.
No exceptions, fabricated hashes, modified security policy or skipped hooks were
used. Actual command strings and canonical policy definitions remain separate in
native-generated execution lineage; the two integration policy IDs refer to the
same complete real PostgreSQL execution.

Final delivery remains blocked by unresolved Wiki verification, independently of
runtime success. After restoring exact change links, native validate passed all
other gates with0lineage issues. The mapped critique also detected context receipt
drift because this findings report had been extended after its trust assessment;
native context-security verify identified this exact file. The drift is an
ordering error, not a new runtime failure. Final source text is now frozen before
refreshing derived Wiki artifacts, source reviews and context security evidence.
The final native outputs are saved separately from both earlier rejection logs.
Context scanner findings are preserved: synthetic malicious-prefix test data in
Test-LlmWikiTools.ps1 and a role-override false positive on the ordinary phrase
`contract as executed checks` in evidence-bundle.md. No scanner policy is relaxed.

This is a checkpoint with completed physical extraction and passing runtime
verification, not an all-green Wiki/delivery certificate or approval to merge.
A normal hook-enabled commit and its actual SHA/clean status are reported in the
handoff; this report deliberately does not invent its own future commit hash.

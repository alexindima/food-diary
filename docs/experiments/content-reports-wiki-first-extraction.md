# ContentReports Wiki-first extraction report

The generated module page correctly identified the application, abstractions, persistence, endpoint, Admin/Users dependency, and focused tests. `start` correctly selected an architectural route and required design.

TypeScript prerequisites were unavailable, forcing the read-only JSON baseline. `research` grounded one path but claimed high confidence and returned unrelated security precedents; `brief` reported risk 0 with no scopes/tests/checks; `test-plan` returned zero scenarios; `ownership` over-ranked frontend moderation files. These are reproducible for a planned module path that does not exist yet.

Useful evidence came from `start`, `design`, the generated module page, direct source/project inspection, architecture tests, and existing extracted modules. Recommended fixes: resolve planned roots to donor projects/manifests, feed module-page tests into briefs, downgrade confidence when no workspace paths or compiled graph exist, and rank backend evidence for backend extraction intents.
